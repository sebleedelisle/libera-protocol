# Protocol Notes

The current working requirements live in Libera while the protocol is being
split out. This repository owns the implementation scaffold and will become the
home for the full wire-format specification as the MVP stabilizes.

